[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ex-skill-plus"
version = "0.1.0"
description = "Generic evidence-bounded chat persona and relationship Agent Skill"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
ocr = ["rapidocr_onnxruntime>=1.3", "Pillow>=10.0"]
